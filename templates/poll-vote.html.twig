{#
/**
 */
#}

<div class="poll">
    <div class="vote-form">

        {% if block %}
        <h3 class="poll-title">{{ title }}%</h3>
        {% endif %}
        {{ choice }}

        {{ vote }}
    </div>
    {{ rest }}
</div>